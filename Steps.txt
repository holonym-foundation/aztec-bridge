Steps:

zsh -i <(curl -s https://install.aztec.network)


cd aztec-contracts/token_bridge
aztec-nargo compile
aztec codegen target -o src/artifacts


cd aztec-contracts/nft_contract
aztec-nargo compile
aztec codegen target -o src/artifacts

cd aztec-contracts/token
aztec-nargo compile
aztec codegen target -o src/artifacts


aztec --version
VERSION=1.0.0-staging.2 aztec --version


source .env
aztec start --port 8081 --pxe --pxe.nodeUrl=$BOOTNODE --pxe.proverEnabled true --l1-chain-id $L1_CHAIN_ID

docker pull aztecprotocol/aztec:1.0.0-staging.2
VERSION=1.0.0-staging.2 aztec start --sandbox

VERSION=1.0.0-staging.2 aztec start --port 8081 --pxe --pxe.nodeUrl=$BOOTNODE --pxe.proverEnabled true --l1-chain-id $L1_CHAIN_ID

